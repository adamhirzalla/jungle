<ul class="list-group">
  <% @categories.each do |category| %>
    <li class="list-group-item"><%= link_to category.name, category %>
    <span class="badge"><%= category.products.count %></span>
    </li>
  <% end %>
</ul>